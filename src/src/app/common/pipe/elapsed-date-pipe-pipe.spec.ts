import { ElapsedDatePipePipe } from './elapsed-date-pipe-pipe';

describe('ElapsedDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ElapsedDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
